/**
 * Export Models, APIs, Flows, Tables, Tasks, Schedules, etc.
 */

/**
 * Export API
 */
function APIs() {
  return { dyform: {} };
}

/**
 * Export Models
 * @param {*} name
 * @param {*} dsl
 */
function Models(name, dsl) {
  //   console.log(dsl);
}
